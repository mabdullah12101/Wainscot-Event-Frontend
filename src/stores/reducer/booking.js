const initialState = {
  data: {},
  isLoading: false,
};

const bookings = (state = initialState, action) => {
  switch (action.type) {
    default: {
      return state;
    }
  }
};

export default bookings;
